# Required — generate with: openssl rand -hex 32
AGENTBRIDGE_TOKEN=replace-with-strong-random-token

# Server
PORT=4000
NODE_ENV=production

# Paperclip integration (optional — omit for mock mode)
PAPERCLIP_API_URL=
PAPERCLIP_API_TOKEN=
